# 旅行タイムテーブル作成アプリ 仕様書

## 1. 文書情報

| 項目 | 内容 |
| --- | --- |
| プロジェクト名 | Trip Timeline（仮称） |
| 文書種別 | Webアプリケーション仕様書 |
| バージョン | 0.1.0 |
| 対象 | MVP（Minimum Viable Product） |
| 使用言語 | HTML / CSS / JavaScript |
| 動作環境 | PC・タブレット・スマートフォンの主要ブラウザ |

## 2. 概要

旅行の日程、訪問先、滞在時間、移動時間などを入力すると、日付ごとのタイムテーブルを自動作成するWebアプリケーション。

ユーザーは作成された予定を確認し、時刻や順番を編集できる。MVPではサーバーを使用せず、入力内容と作成済み旅行プランをブラウザの `localStorage` に保存する。

## 3. 目的

- 旅行中の行動予定を時系列で簡単に整理できるようにする
- 訪問先ごとの滞在時間と移動時間を含む、実用的な予定表を自動作成する
- スマートフォンでも旅行中に見やすく、編集しやすい画面を提供する
- 外部サービスへの登録なしで、すぐに利用できるようにする

## 4. 想定ユーザー

- 個人旅行の予定を立てる人
- 家族、友人、カップルなど少人数の旅行を計画する人
- 紙や表計算ソフトより手軽に、時刻付きの旅程を作りたい人

## 5. MVPの対象範囲

### 5.1 対象に含める機能

- 旅行の基本情報の入力
- 旅行期間内の日付ごとの予定入力
- 訪問先、滞在時間、移動時間の登録
- 入力情報からのタイムテーブル自動作成
- 作成結果の表示
- 予定の追加、編集、削除、並べ替え
- 日をまたぐ予定や営業時間外などの警告表示
- ブラウザへの自動保存
- 保存済み旅行プランの再表示と削除
- 印刷用レイアウト
- レスポンシブ表示

### 5.2 MVPでは対象外とする機能

- ユーザー登録、ログイン
- サーバーやデータベースへの保存
- 複数ユーザーによる共同編集
- Google Mapsなどを利用した移動時間の自動取得
- 交通機関の経路検索、運行情報との連携
- 施設の営業時間や定休日の自動取得
- 航空券、ホテル、飲食店などの予約
- AIによる観光地の自動提案
- PDFファイルの直接生成

## 6. 前提条件

- 1つの旅行プランには、1日以上の日程がある
- 旅行期間は開始日から終了日までの連続した日付とする
- 各日の開始時刻はユーザーが設定する
- 訪問先間の移動時間はユーザーが入力する
- タイムテーブルは入力された訪問先の順番を基本として作成する
- 時刻の計算は端末のローカル時刻を基準とし、MVPではタイムゾーン変換を行わない
- データは同一ブラウザ、同一端末内にのみ保存される

## 7. ユーザーフロー

1. ユーザーがトップ画面を開く
2. 「新しい旅行を作成」を選択する
3. 旅行名、目的地、開始日、終了日を入力する
4. 日付ごとに開始時刻と訪問先を入力する
5. 各訪問先の滞在時間と、次の訪問先までの移動時間を入力する
6. 「タイムテーブルを作成」を選択する
7. アプリが到着時刻、滞在時間、出発時刻、移動時間を計算する
8. ユーザーが作成結果を確認し、必要に応じて編集する
9. 旅行プランがブラウザに自動保存される
10. ユーザーは後から保存済みプランを開く、印刷する、または削除する

## 8. 機能要件

### 8.1 旅行プラン管理

#### 新規作成

以下の基本情報を入力できること。

| 項目 | 必須 | 入力形式 | 制約 |
| --- | --- | --- | --- |
| 旅行名 | 必須 | 文字列 | 1〜50文字 |
| 主な目的地 | 必須 | 文字列 | 1〜100文字 |
| 開始日 | 必須 | 日付 | 終了日以前 |
| 終了日 | 必須 | 日付 | 開始日以後 |
| メモ | 任意 | 複数行文字列 | 500文字以内 |

#### 保存

- 入力内容に変更があった場合、自動保存する
- 自動保存が完了したことを画面上に表示する
- 保存データには一意なID、作成日時、更新日時を含める
- 保存済みプランは更新日時の新しい順で一覧表示する

#### 削除

- 保存済みプランを削除できる
- 誤操作防止のため、削除前に確認ダイアログを表示する
- 削除後は元に戻せないことを確認文に明記する

### 8.2 日程入力

旅行期間から日付タブまたは日付セクションを自動生成する。

各日について以下を設定できること。

| 項目 | 必須 | 初期値 | 制約 |
| --- | --- | --- | --- |
| 1日の開始時刻 | 必須 | 09:00 | 00:00〜23:59 |
| 1日の終了目安 | 任意 | 18:00 | 開始時刻より後を推奨 |
| 出発地点 | 任意 | 空欄 | 100文字以内 |
| 日ごとのメモ | 任意 | 空欄 | 500文字以内 |

### 8.3 訪問先の入力

日付ごとに0件以上の訪問先を登録できること。

| 項目 | 必須 | 入力形式 | 初期値・制約 |
| --- | --- | --- | --- |
| 訪問先名 | 必須 | 文字列 | 1〜100文字 |
| カテゴリ | 任意 | 選択 | 観光、食事、宿泊、買い物、移動、その他 |
| 滞在時間 | 必須 | 分 | 60分、5〜1,440分 |
| 次の場所までの移動時間 | 必須 | 分 | 0分、0〜1,440分 |
| 時刻固定 | 任意 | チェック | 初期値は無効 |
| 固定開始時刻 | 条件付き必須 | 時刻 | 「時刻固定」が有効な場合のみ |
| 住所・場所 | 任意 | 文字列 | 200文字以内 |
| メモ | 任意 | 複数行文字列 | 500文字以内 |

訪問先に対して以下の操作ができること。

- 追加
- 編集
- 複製
- 削除
- 上へ移動
- 下へ移動

ドラッグ＆ドロップによる並べ替えは拡張機能とし、MVPでは上下ボタンによる操作を必須とする。

### 8.4 タイムテーブルの自動作成

#### 基本計算

各日の開始時刻を基準として、入力順に以下を計算する。

```text
訪問先の到着時刻 = 直前の訪問先の出発時刻 + 移動時間
訪問先の出発時刻 = 訪問先の到着時刻 + 滞在時間
```

1件目の到着時刻は、その日の開始時刻とする。出発地点から1件目までの移動を設定する場合は、1件目の前に「移動」予定として登録する。

#### 時刻固定の予定

- 時刻固定の予定は、指定された開始時刻に配置する
- 直前の予定が固定開始時刻より早く終わる場合、空き時間を「自由時間」として表示する
- 直前の予定が固定開始時刻を超える場合、予定の重複として警告する
- 重複があってもタイムテーブル自体は表示し、ユーザーが修正できるようにする

#### 日付をまたぐ予定

- 計算後の時刻が24:00を超える場合、翌日であることが分かる表示にする
- 例: `翌 00:30`
- 旅行期間の最終日を越える場合は警告を表示する

#### 再計算

- 訪問先の順番、滞在時間、移動時間、固定時刻、1日の開始時刻を変更した場合、再計算できること
- 「タイムテーブルを更新」操作で結果へ反映する
- 未保存の入力が失われないこと

### 8.5 タイムテーブル表示

日付ごとに、以下の情報を時系列で表示する。

- 到着または開始時刻
- 出発または終了時刻
- 訪問先名
- カテゴリ
- 滞在時間
- 次の場所までの移動時間
- 住所・場所
- メモ
- 警告

表示例:

```text
09:00 ─ 10:30  清水寺
                 観光 / 滞在 90分
10:30 ─ 11:00  移動（30分）
11:00 ─ 12:00  京都駅周辺で昼食
                 食事 / 滞在 60分
```

### 8.6 警告

以下の場合に、対象箇所と画面上部へ警告を表示する。

- 必須項目が入力されていない
- 開始日が終了日より後になっている
- 固定時刻の予定に固定開始時刻が設定されていない
- 予定同士が重複している
- 予定が1日の終了目安を超えている
- 予定が翌日にまたがっている
- 最終日の予定が旅行期間外まで続いている
- `localStorage` への保存に失敗した

警告は入力内容を勝手に変更せず、理由と修正対象を示す。

### 8.7 印刷

- ブラウザの印刷機能を使用してタイムテーブルを印刷できる
- 印刷時は入力フォーム、編集ボタン、ナビゲーションを非表示にする
- 旅行名、目的地、日付、タイムテーブル、メモを印刷対象にする
- A4縦向きを基本とし、日付の途中で内容が不自然に分割されにくいCSSを設定する

## 9. 画面仕様

### 9.1 トップ・旅行一覧画面

#### 表示内容

- アプリ名と簡単な説明
- 「新しい旅行を作成」ボタン
- 保存済み旅行プラン一覧
- 各プランの旅行名、目的地、旅行期間、更新日時
- 「開く」ボタン
- 「削除」ボタン

#### 空状態

保存済みプランがない場合は、アプリの使い方と「最初の旅行を作成」ボタンを表示する。

### 9.2 旅行情報入力画面

#### 表示内容

- 旅行基本情報フォーム
- 日付ごとの入力セクション
- 訪問先カード一覧
- 訪問先追加ボタン
- タイムテーブル作成ボタン
- 保存状態
- 一覧へ戻るボタン

### 9.3 タイムテーブル画面

#### 表示内容

- 旅行名、目的地、旅行期間
- 日付切り替え
- 日付ごとのタイムライン
- 警告一覧
- 編集画面へ戻るボタン
- タイムテーブル更新ボタン
- 印刷ボタン

### 9.4 削除確認ダイアログ

#### 表示内容

- 削除対象の旅行名
- 削除後は元に戻せない旨
- キャンセルボタン
- 削除実行ボタン

## 10. UI・デザイン要件

- 旅行の楽しさを感じられる、明るく落ち着いた配色にする
- 情報量が多くても読みやすいカード型レイアウトを使用する
- メイン操作と補助操作を色、サイズ、配置で区別する
- 現在選択中の日付を明確にする
- カテゴリごとにアイコンまたは色を付ける。ただし色だけに依存しない
- 時刻を視線の起点にできるタイムライン形式にする
- 保存中、保存済み、保存失敗の状態を文字で表示する
- ボタン、入力欄はタッチ操作しやすい大きさにする
- スマートフォンでは1カラム、広い画面では入力とプレビューの2カラム表示を検討する

## 11. レスポンシブ要件

| 画面幅 | 基本レイアウト |
| --- | --- |
| 〜767px | 1カラム、日付は横スクロール可能、主要ボタンは画面幅に合わせる |
| 768〜1,023px | 1〜2カラム、カード幅を調整 |
| 1,024px〜 | 入力とタイムテーブルの2カラム表示が可能 |

横スクロールは日付タブなど限定した要素でのみ許容し、ページ全体には発生させない。

## 12. アクセシビリティ要件

- HTMLの見出し構造を正しく使用する
- 入力欄にはすべて表示ラベルを関連付ける
- キーボードだけですべての主要操作を実行できる
- フォーカス位置を視覚的に確認できる
- エラー内容を色だけでなく文字でも伝える
- 文字と背景は十分なコントラストを確保する
- ダイアログ表示時はフォーカスをダイアログ内に移動する
- 動的な保存状態やエラーは必要に応じて `aria-live` で通知する
- `prefers-reduced-motion` が有効な場合は不要なアニメーションを抑える

## 13. データ仕様

### 13.1 データモデル

```js
{
  id: "trip_uuid",
  title: "京都2泊3日",
  destination: "京都",
  startDate: "2026-10-10",
  endDate: "2026-10-12",
  memo: "紅葉と寺社巡り",
  createdAt: "2026-07-24T10:00:00.000Z",
  updatedAt: "2026-07-24T10:30:00.000Z",
  days: [
    {
      date: "2026-10-10",
      startTime: "09:00",
      targetEndTime: "18:00",
      departurePoint: "京都駅",
      memo: "",
      places: [
        {
          id: "place_uuid",
          name: "清水寺",
          category: "sightseeing",
          durationMinutes: 90,
          travelMinutesToNext: 30,
          isFixedTime: false,
          fixedStartTime: null,
          location: "京都市東山区",
          memo: ""
        }
      ]
    }
  ]
}
```

計算によって求められる到着時刻、出発時刻、空き時間、警告は原則として保存せず、表示時に再計算する。

### 13.2 カテゴリ値

| 表示名 | 保存値 |
| --- | --- |
| 観光 | `sightseeing` |
| 食事 | `food` |
| 宿泊 | `lodging` |
| 買い物 | `shopping` |
| 移動 | `transport` |
| その他 | `other` |

### 13.3 保存方式

- `localStorage` のキー: `trip-timeline:trips:v1`
- 値: 旅行プラン配列をJSON文字列化したもの
- データ形式変更に備え、キーにバージョンを含める
- JSONの解析に失敗した場合はアプリ全体を停止させず、復旧案内を表示する
- 保存容量を超えた場合は、不要なプラン削除を促す

## 14. 技術仕様

### 14.1 構成

ビルドツールやフレームワークを使わない、静的Webアプリケーションとして実装する。

```text
/
├── index.html
├── css/
│   ├── style.css
│   └── print.css
├── js/
│   ├── app.js
│   ├── storage.js
│   ├── timetable.js
│   ├── validation.js
│   └── ui.js
├── assets/
│   └── icons/
└── travel-timetable-app-spec.md
```

### 14.2 各ファイルの責務

| ファイル | 責務 |
| --- | --- |
| `index.html` | 画面の基本構造、セマンティックHTML |
| `style.css` | 共通デザイン、レスポンシブ対応 |
| `print.css` | 印刷専用スタイル |
| `app.js` | アプリ初期化、画面遷移、各処理の連携 |
| `storage.js` | `localStorage` の読み書き、データ更新 |
| `timetable.js` | 時刻計算、固定予定、空き時間、警告の生成 |
| `validation.js` | 入力値検証 |
| `ui.js` | DOM生成、イベント処理、画面更新 |

### 14.3 実装方針

- JavaScriptはES Modulesを使用する
- HTML内に大きなJavaScriptやCSSを直接記述しない
- 時刻計算は「日付＋分数」に正規化し、文字列のまま加算しない
- ユーザーが入力した文字列を `innerHTML` に直接挿入しない
- DOM要素のIDは一意にする
- 状態データを単一の旅行プランオブジェクトとして管理する
- 表示更新とデータ更新の責務を分ける
- 外部ライブラリは原則使用しない

## 15. 入力検証

### 15.1 検証タイミング

- 入力欄からフォーカスが外れたとき
- タイムテーブル作成時
- 保存時

### 15.2 エラー表示

- 対象入力欄の近くに具体的なエラーメッセージを表示する
- 最初のエラー項目へ移動できるようにする
- 入力途中の値は、明らかに不正でない限り勝手に削除しない
- エラーが解消されたら表示を解除する

### 15.3 主な検証ルール

- 必須文字列は空白のみを許可しない
- 日付は有効なISO形式であること
- 終了日は開始日以後であること
- 時刻は `HH:mm` 形式であること
- 分数は整数で、項目ごとの範囲内であること
- 固定予定には固定開始時刻があること
- 旅行期間の変更時は、削除対象となる日程がある場合に確認すること

## 16. 非機能要件

### 16.1 パフォーマンス

- 初回表示は一般的なモバイル回線で3秒以内を目標とする
- 1旅行につき30日、1日につき50件程度の予定を扱えること
- 入力操作中に体感できる遅延を発生させない

### 16.2 対応ブラウザ

公開時点における以下の最新版から2世代前までを目安とする。

- Google Chrome
- Microsoft Edge
- Mozilla Firefox
- Safari
- iOS Safari
- Android Chrome

### 16.3 セキュリティとプライバシー

- 入力データは外部へ送信しない
- ユーザー入力をHTMLとして実行しない
- 外部リンクを新しいタブで開く場合は `rel="noopener noreferrer"` を設定する
- 保存先がブラウザ内であり、端末の共有者から閲覧される可能性があることを案内する
- 機密情報や予約番号の保存を推奨しない

### 16.4 可用性

- JavaScriptエラーが発生しても、可能な範囲で保存済みデータを失わない
- 保存処理の失敗をユーザーに通知する
- 不正な保存データを検出した際に、データを自動削除しない

## 17. 受け入れ条件

### 17.1 基本操作

- [ ] 必須項目を入力して新しい旅行プランを作成できる
- [ ] 開始日から終了日までの日付セクションが生成される
- [ ] 各日に複数の訪問先を登録できる
- [ ] 訪問先を編集、複製、削除、並べ替えできる
- [ ] 入力順にタイムテーブルが作成される
- [ ] 滞在時間と移動時間が正しく時刻へ反映される
- [ ] 固定時刻までの空き時間が表示される
- [ ] 固定時刻と前の予定が重複した場合に警告される
- [ ] 1日の終了目安を超えた場合に警告される
- [ ] 翌日にまたがる時刻が分かりやすく表示される

### 17.2 保存

- [ ] ページを再読み込みしても保存済みプランを開ける
- [ ] 複数の旅行プランを保存できる
- [ ] 保存済みプランが更新日時順に表示される
- [ ] 確認後に旅行プランを削除できる
- [ ] 保存失敗時にエラーメッセージが表示される

### 17.3 表示と操作性

- [ ] 幅320px程度の画面でもページ全体に横スクロールが発生しない
- [ ] キーボードのみで主要操作を完了できる
- [ ] 入力エラーの場所と理由を文字で確認できる
- [ ] 印刷時に編集用UIが非表示になる
- [ ] PCとスマートフォンの両方でタイムテーブルが読みやすい

## 18. テスト観点

### 18.1 単体テスト相当

- 23:30に60分滞在した場合、終了時刻が `翌 00:30` になる
- 09:00開始、90分滞在、30分移動の場合、次の予定が11:00開始になる
- 10:00固定の予定に対し前の予定が09:30に終わる場合、30分の自由時間が生成される
- 10:00固定の予定に対し前の予定が10:30に終わる場合、30分の重複警告が生成される
- うるう年を含む旅行期間の日付が正しく生成される
- 空欄、負数、小数、上限超過の分数が拒否される

### 18.2 結合テスト相当

- 旅行作成から保存、再読み込み、再編集までを完了できる
- 旅行期間を延長すると新しい日付セクションが追加される
- 旅行期間を短縮すると削除対象の日程について確認される
- 並べ替え後にタイムテーブルの時刻が再計算される
- 保存容量超過を想定した場合にデータを保持したまま警告される

### 18.3 表示テスト

- 320px、768px、1,024px、1,440pxの各幅で確認する
- 長い旅行名、長い訪問先名、長いメモでレイアウトが崩れない
- ダークモードはMVP必須ではないが、OS設定時も内容が読めることを確認する
- 印刷プレビューで日付ごとの内容を確認する

## 19. 将来拡張

- Google Maps Platformなどによる移動時間の自動取得
- 現在地から次の訪問先までのナビゲーション
- 交通機関、天気、営業時間との連携
- AIによる訪問順と所要時間の提案
- アカウント登録とクラウド同期
- 旅行メンバーとの共有、共同編集
- JSON、CSV、PDF、カレンダー形式での出力
- 予約情報、予算、持ち物リストの管理
- オフライン対応（PWA）
- 多言語対応
- タイムゾーン対応

## 20. 実装優先順位

### フェーズ1: 基本入力と計算

- 静的な画面構成
- 旅行基本情報
- 日付生成
- 訪問先の追加、編集、削除、並べ替え
- タイムテーブル計算

### フェーズ2: 保存と編集

- `localStorage` 保存
- 旅行一覧
- 保存済みプランの再編集
- 削除確認
- 入力検証と警告

### フェーズ3: 仕上げ

- レスポンシブ対応
- アクセシビリティ対応
- 印刷スタイル
- エラー処理
- ブラウザ横断テスト

## 21. MVP完了の定義

受け入れ条件をすべて満たし、ユーザーが1台の端末上で「旅行の作成」「訪問先の入力」「タイムテーブルの自動作成」「予定の修正」「保存済み旅行の再表示」「印刷」を問題なく完了できる状態をMVPの完成とする。
